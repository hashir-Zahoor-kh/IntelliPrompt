# Node.js Scanner Prompt

Analyze this Node.js project:

- Check package.json for dependencies
- Identify framework (React, Vue, Next, Express, etc.)
- Detect build tool (Vite, Webpack, etc.)
- Find entry points
- Identify deployment requirements
